/**
 * Pre-Spring standalone SMTP smoke test.
 * Superseded by {@code blinov_first.service.impl.MailServiceImplTest}
 * (uses GreenMail in-memory SMTP server).
 *
 * @deprecated Use Spring Boot / GreenMail tests instead.
 */
@Deprecated
public class EmailTest {
    // Removed — Spring Boot auto-configures JavaMailSender.
}
