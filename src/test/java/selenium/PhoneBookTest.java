package selenium;

/**
 * Pre-Spring Selenium phone book tests using /controller?command= URLs.
 * Replaced by {@code blinov_first.selenium.PhoneBookTest}.
 *
 * @deprecated Use {@code blinov_first.selenium.PhoneBookTest} instead.
 */
@Deprecated
class PhoneBookTest extends BaseSeleniumTest {
    // Removed — replaced by blinov_first.selenium.PhoneBookTest with Spring MVC URLs.
}
