<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!--
        Spring Boot WAR — this file is intentionally minimal.

        All servlet registration, filter chains and security rules are
        configured in Java via:
            BlinovApplication  (SpringBootServletInitializer)
            SecurityConfig     (Spring Security filter chain)
            AppConfig          (application beans)

        The old ConnectionPool context-params, EncodingFilter, FlashFilter,
        and SecurityFilter declarations have been removed; their functions
        are now handled by:
            - HikariCP          (connection pooling via spring.datasource.*)
            - Spring MVC        (CharacterEncodingFilter auto-configured)
            - RedirectAttributes (flash messages)
            - Spring Security   (SecurityConfig)
    -->

    <!--
        Error pages are still read by the Servlet container even in
        Spring Boot WAR mode, so we keep them here as a safety net.
        The primary exception handling is done by GlobalExceptionHandler.
    -->
    <error-page>
        <error-code>403</error-code>
        <location>/pages/error/error_403.jsp</location>
    </error-page>
    <error-page>
        <error-code>404</error-code>
        <location>/pages/error/error_404.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/pages/error/error_500.jsp</location>
    </error-page>
    <error-page>
        <exception-type>java.lang.Throwable</exception-type>
        <location>/pages/error/error_500.jsp</location>
    </error-page>

</web-app>
