package blinov_first.command;

/**
 * Enum mapping URL command names to {@link Command} instances.
 *
 * Replaced by Spring MVC {@code @RequestMapping} / {@code @GetMapping} /
 * {@code @PostMapping} annotations on controller classes.
 *
 * @deprecated Replaced by Spring MVC URL routing.
 */
@Deprecated
public enum CommandType {
    ;   // No constants — all routing is handled by Spring MVC.
}
